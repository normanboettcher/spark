package de.basic.modules;

import java.util.HashMap;

import org.apache.spark.SparkConf;
import org.apache.spark.sql.Dataset;
import org.apache.spark.sql.Row;
import org.apache.spark.sql.SparkSession;

import com.google.common.collect.HashBiMap;

/**
 * 
 * @author norman
 *
 */
public class Statistics {
	
	public static HashMap<String, ? extends Object> calcStatisticsForDF(SparkSession s, 
			Dataset<Row> data) {
		HashMap<String, ? extends Object> result = new HashMap<>();
		
		/*
		 * calculate different statistics
		 * 
		 * save statistics in HashMap
		 * 
		 * return HashMap
		 * 
		 */
		return result;
		
	}
}
